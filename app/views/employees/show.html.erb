<h1> <%= @employee.name %> </h1> 

<h3> Alias: <%= @employee.alias %> </h3> 

<h3> Title: <%= @employee.title %> </h3> 

<h3> Office: <%= @employee.office %> </h3> 

 <%= image_tag(@employee.img_url) %> </br>

 <h3>Dog they belong to: <%= link_to @employee.dog.name, dog_path(@employee.dog)%> </h3> 

<%= link_to "Edit", edit_employee_path(@employee) %> 

<%= link_to "Delete", employee_path(@employee), method: :delete %> 